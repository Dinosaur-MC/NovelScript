# Backend API base URL — copy to .env.development and adjust if needed.
# Dev only: browser calls API directly.  Production: SSR server proxies
# /api/v1/* internally; leave this variable unset for relative paths.
VITE_API_BASE_URL=http://localhost:8000/api/v1
